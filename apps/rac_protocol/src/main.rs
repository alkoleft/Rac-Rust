fn main() {
    eprintln!("rac_protocol: placeholder binary. Use subcommands in src/bin.");
    eprintln!("Examples:");
    eprintln!("  cargo run -p rac_protocol --bin rac_lite -- agent-version 127.0.0.1:1545");
    eprintln!("  cargo run -p rac_protocol --bin rac_decode -- logs/<session>/client_to_server.stream.bin");
}
